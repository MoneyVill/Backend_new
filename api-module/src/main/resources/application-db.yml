# MongoDB 설정
spring:
  data:
    mongodb:
      uri: mongodb+srv://sparta:jungle@cluster0.hnot7ve.mongodb.net/Newtest?retryWrites=true&w=majority&appName=Cluster0
      authentication-database: admin
---

# 개발 환경(DB Dev) 설정
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/money
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
    hikari:
      connection-timeout: 58000
      max-lifetime: 580000
